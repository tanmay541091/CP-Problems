#include <bits/stdc++.h>
#include <string>
using namespace std;

int main() {
    int t, n, j;
    string s, p;
    cin >> t;
    while (t--){
        cin >> n;
        cin >> s;
        for (int i = 0, i < n, i++){
            p = s.at(i);
            q = s.at(i+1);
            j = s.find(p)
        }d          
    }
    return 0;
}